import 'package:flutter/material.dart';

class CustomiseTabletView extends StatelessWidget {
  const CustomiseTabletView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
